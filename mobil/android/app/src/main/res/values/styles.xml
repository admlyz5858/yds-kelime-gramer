<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Android 15 (API 35) edge-to-edge zorunluluğundan çık: içerik durum/navigasyon
             çubuğunun altına girmesin, üst bar butonları tıklanabilir kalsın. -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
        <!-- Durum ve navigasyon çubuğu uygulamanın krem temasıyla uyumlu, koyu ikonlu -->
        <item name="android:statusBarColor">#f4ecdb</item>
        <item name="android:navigationBarColor">#f4ecdb</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>